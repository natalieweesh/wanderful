<!DOCTYPE html>
<html>
<head>
  <title>Capstone</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  
</head>
<body>
  <div class="container">
  
  <% if user_signed_in? %>
    <%= button_to "sign out", destroy_user_session_url, method: :delete, class: "logout-button" %>
  <% end %>
  
  <%= button_to "home", root_url %>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  
<%= yield %>

  </div>
</body>
</html>
